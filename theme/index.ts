import colors from './colors/colors';
import radiuses from './radiuses/radiuses';
import spacing from './spacing/spacing';

const theme = {
  colors,
  spacing,
  radiuses,
} as const;

export default theme;
