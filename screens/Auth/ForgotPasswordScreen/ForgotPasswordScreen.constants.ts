import { EmailFormProps, MobileNumberFormProps } from './ForgotPasswordScreen.types';

export const INITIAL_RESET_EMAIL_FORM_VALUES: EmailFormProps = {
  email: '',
};

export const INITIAL_RESET_MOBILE_NUMBER_FORM_VALUES: MobileNumberFormProps = {
  mobileNumber: '',
};

export const ADDITIONAL_KEYBOARD_OFFSET = 80;
